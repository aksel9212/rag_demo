Wenn als Nachtabsenkart "Abschaltbetrieb" eingestellt ist, wird die Heizung nachts abgeschaltet. In diesem Fall kann keine Nacht-Raumtemperatur eingestellt werden, und das Display zeigt eine entsprechende Meldung an (wie im letzten Absatz der vorherigen Seite erwähnt).